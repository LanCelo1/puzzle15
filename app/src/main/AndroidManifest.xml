<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uz.gita.tosh15">

    <application
        android:name="uz.gita.tosh15.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Puzzle15_v1">
        <activity android:name="uz.gita.tosh15.MainActivity3"></activity>
        <activity android:name="uz.gita.tosh15.AboutActivity" />
        <activity android:name="uz.gita.tosh15.MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="uz.gita.tosh15.MainActivity" />
    </application>

</manifest>